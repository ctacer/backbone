{
  "name": "backbone-trainig",
  "version": "0.0.1",
  "description": "Backbone js training app",
  "dependencies": {
    "body-parser": "^1.8.2",
    "express": "^4.9.0",
    "http-proxy": "^1.4.3",
    "restler": "^3.2.2"
  },
  "scripts": {
    "postinstall": "bower install",
    "prestart": "npm install",
    "start": "node server"
  }
}
